apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: movierecom-ingress

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: movierecom-service

                port:
                  number: 80